import { NextFunction, Request, Response } from "express";
import Client from "../../modules/auth/model/client";

export default function getClient(req: Request, res: Response, next: NextFunction) {
}